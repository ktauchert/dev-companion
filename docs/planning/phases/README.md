# Phasen — Umsetzungspläne

Der [Phasenplan](../phasenplan.md) bleibt die **Reihenfolge** (was als Nächstes, Fertig-wenn in einer Zeile). Hier liegen die **Umsetzungspläne** pro Arbeitspaket: was genau, was nötig ist, was bewusst draußen bleibt.

Nur das aktuelle Paket umsetzen, und nur nach coding-Trigger (`code`, `execute`, `umsetzen`, `make it so`).

| Phase | Pläne |
| --- | --- |
| 0 Orientierung | erledigt in Docs, kein Ordner |
| 1 Fundament | [phase1/](phase1/) — ab AP 1.3 |
| 2–6 | noch nicht ausgearbeitet |

AP 1.1 und AP 1.2 sind umgesetzt. Stolpersteine: [lessons-learned/phase-1](../../lessons-learned/phase-1/).
